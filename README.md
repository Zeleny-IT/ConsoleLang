# Console
Console is a high-level programming language

# Hello world in Console
```
print("Hello World!")
```

# Define the function
```
func add(left, right) {
    return float(left) + float(right)
  }
```
